server {
    listen 80;
    server_name <%= @domain %>;

    access_log /var/log/nginx/<%= @title %>.access.log;
    error_log /var/log/nginx/<%= @title %>.error.log;

    location / {
        root <%= @path %>;
    }
}
